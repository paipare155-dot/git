#git commands to practice


git init - to intiallise the git repo

git add filename - to add file in stagging

git status - chekc the status of the file whether the file is added in stagging or not or commited or not.

git commit -m " "  - to commit the files to git vcs on you local.

git log - checks the commit history or latest commits.

git remote add origin repo_url - To add remote repo to push the chnages to the specific repo.

git branch branchname - to create new branch

git checkout branchname - to switch between brah=nches to push chnages to specific branch.

git push -u origin branchname - push the current commited chnages to the github repo or remote repo.

git remote -v - check which remote repository your local Git repository is connect to.

git clone repourl - if you want the remote repo to you local repo.

git pull repourl - to pull the changes from remote repo to local.
